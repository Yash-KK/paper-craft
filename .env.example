# Copy to .env and fill in real values
# cp .env.example .env

# OpenAI — dense embeddings + chat
OPENAI_API_KEY=sk-dummy-openai-key

# Qdrant — vector store (local or cloud)
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=

# Mistral — PDF OCR extraction (notebooks only)
MISTRAL_API_KEY=dummy-mistral-key

# Optional API guard for /api/v1/* endpoints (leave empty to disable)
API_KEY=
